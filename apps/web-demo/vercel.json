{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "curl -fsSL https://bun.sh/install | bash -s \"bun-v1.3.0\" && BUN_INSTALL=\"$HOME/.bun\" PATH=\"$BUN_INSTALL/bin:$PATH\" && bun install --frozen-lockfile",
  "buildCommand": "BUN_INSTALL=\"$HOME/.bun\" PATH=\"$BUN_INSTALL/bin:$PATH\" turbo run build --filter web-demo",
  "env": {
    "CSS_TRANSFORMER_WASM": "1"
  }
}
