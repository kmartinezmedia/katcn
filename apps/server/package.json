{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "clean": "cli tools clean",
    "css": "katcn css --include docgen --outFile ./.katcn/styles.css",
    "prebuild": "NODE_ENV=production bun run ./scripts/prebuild.ts",
    "dev": "NODE_ENV=production bun run --hot src/index.tsx",
    "fix": "cli tools fix",
    "lint": "cli tools lint",
    "test": "bun test",
    "typecheck": "cli tools typecheck"
  },
  "imports": {
    "#dtsLibs": {
      "types": "./src/dtsLibs.d.ts",
      "default": "./dist/dtsLibs.json"
    }
  },
  "dependencies": {
    "base64-url": "^2.3.3",
    "hono": "^4.0.10",
    "katcn": "workspace:*",
    "ts-morph": "^22.0.0"
  },
  "devDependencies": {
    "@types/base64-url": "^2.2.2"
  }
}
